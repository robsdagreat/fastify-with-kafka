version: '3.8'

services:
  backend:
    build:
      context: ./backend
      target: production  # Multi-stage build
    environment:
      NODE_ENV: production

  frontend:
    build:
      context: ./frontend
      target: production  # Multi-stage build
    environment:
      NODE_ENV: production