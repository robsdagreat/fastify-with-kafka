version: '3.8'

services:
  backend:
    volumes:
      - ./backend/src:/app/src  # Live code reload
      - /app/node_modules       # Isolate container dependencies
    command: sh -c "npm install && npm run dev"
    environment:
      NODE_ENV: development